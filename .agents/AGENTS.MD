1. Never import dp and sp use dimens
2. Never use hardcoded color always user materials color and appColors and always match dark and light theme color
3. Always use solid clean architecture mvvm and for ui mvi
4. Always use string resource
5. And if icon not found then create xml icon and then never use icon library
6. Never Git Push 
7. If working with database always care about migration and never delete actual database always play safe
8. Always go with the app theme never distract be consistent 
9. Always Plan before working
10. You will check common ui files for reusable component if  not found and if its a common component you will make it usable